<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jadeskynetexer"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:label="@string/app_name"
            android:name="jade.application.FirstActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="jade.application.MainActivity"></activity>
        
        <activity android:name="jade.application.OneActivity"></activity>
        <activity android:name="jade.application.TwoActivity"></activity>
        <activity android:name="jade.application.ThreeActivity"></activity>
    </application>

</manifest>
